#include <stdio.h>

void counter() {
    static int count = 0;   // static variable to store number of calls
    count++;
    printf("Function called %d times\n", count);
}

int main() {
    counter();
    counter();
    counter();
    counter();

    return 0;
}
